# 🧠 AI Quiz Generator (Spring Boot + OpenRouter Integration)

This project uses the [OpenRouter API](https://openrouter.ai/) to generate AI-based quiz questions from lesson content.  
Follow the setup steps below to configure your OpenRouter API key securely.

---

## ⚙️ Environment Setup

### 1️⃣ Create a `.env` file in the project root
Create a new file named `.env` (in the same folder as your `pom.xml`).

Add the following line inside:

```bash
OPENROUTER_API_KEY="your_openrouter_api_key_here"
